
<%= form_with(model: todo_list, local: true) do |form| %>
  <% if todo_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</h2>

      <ul>
      <% todo_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="todo_list_form">
  <div class="field">
    <%= form.text_field :title, id: :todo_list_title, placeholder: "Add a new task", class: "input_field" %>
  </div>

  <div class="field">
    <%= form.text_area :description, id: :todo_list_description, placeholder: "Add a description", class: "input_field" %>
  </div>
  <div class="actions">
    <%= form.submit class: "primary_button" %>
  </div>
  </div>
<% end %>
